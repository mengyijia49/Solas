# Solas

Solas 是一个从零开始手写的推理框架。


## 项目原则

- 不使用第三方运行时依赖。
- 尽量用清晰、可读的代码实现每个模块。
- 每增加一个功能，就补一个对应测试。
- 先做最小可运行版本，再慢慢扩展。
- 重要设计写进文档，方便以后回顾。

- `include/`：放 C API 头文件。
- `src/`：放 C/C++ 后端实现。
- `python/`：放 Python 前端和 ctypes 封装。
- `test/`：放测试代码。
- `docs/`：放设计文档和开发记录。